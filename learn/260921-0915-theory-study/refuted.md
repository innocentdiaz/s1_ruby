# Refuted claims

Each line: the claim as raised, then the reason it was refuted, as given.

- THEORY.md:174-177 vs lib/s1/answer.rb:44, lib/s1/result.rb:44,80 and state.rb THRESHOLD_ON_VERB — "Travels with the measurement" and "per-call" misdescribe where the threshold is set: it is fixed at prepare (State) or supplied at collapse, refused on every measuring verb, and overridable after the fact. — refuted: Every assertion in THEORY.md:174-177 holds in code: config (S1.config.threshold), per-state (State.new/with/to_s1 threshold:), per-call (judge?/is?/same_as?(threshold:), collapse(t), true?(id, t) — all calls), and "a measurement carries the threshold it was taken under" (Client#measure stamps the St
- THEORY.md:199 and 114 vs THEORY.md:301-302 — The provider is defined as the implementation of measure and the doer of judgement, then the gem is said to own the arrow measure and the provider only a translation. — refuted: No contradiction: "owns" at line 301 means owns the definition (the gem fixes what the arrow measure is — Request in, calibrated distributions out), while 199/114 speak of who implements/performs it, and the two are reconciled inside the Provider entry itself, whose *Code* line already says exactly
- THEORY.md:11-13 vs THEORY.md:69, 105-109 (and README.md:123) — The diagram omits the lens, which prepare is defined to attach; the README's version of the same diagram has it. — refuted: The theory defines the lens as evidence ("evidence added to the state to judge against", THEORY.md:106; "if it changes the evidence, it is the lens", :252), so the diagram's leftmost position already contains it: ψ(x) with x = `{ this: …, **lens }` (:40) is literally the code — `State.new(evidence,
- THEORY.md:120 vs THEORY.md:225-226, 201-202 and lib/s1/question.rb:3-6 — "Wire" names two different things: the provider's transport payload and the gem's provider-agnostic Question form. — refuted: Line 120 and line 225 name the same thing: a Question is `{ type: noul|choice|score, instructions, criteria }`, i.e. it is built from exactly the wire names 225 enumerates (`noul`, `choice`, `score`, `criteria`), which 208 and README:859 already say are borrowed provider words, and on the canonical
- THEORY.md:205-209, 225-226, 313 — noul is declared both a theory name (ours) and a wire name (the provider's), contradicting the wire entry's split "wire names are the provider's; theory names are ours"; criteria has the same double status. — refuted: The wire entry (THEORY.md:225-226) states which side each set of names belongs to; it never claims the sets are disjoint, so a word appearing in both is not a contradiction. Noul's double status is already declared twice in the text — the Noul entry (208-209: "Also the wire name ... which is where t
- THEORY.md:56, 131 vs lib/s1/distribution.rb:34-35 — `kind` is the theory's word for the scale kind (dichotomous / nominal / ordinal) but the method `kind` returns the wire name (noul / choice / score). — refuted: THEORY.md:56-57 already states exactly what the probe shows: "`kind`, the scale kind by its wire name" — the accessor is defined as returning noul/choice/score, so `d.kind == "ordinal"` being false is the documented behaviour, not a drift. The glossary at 225-226 lists both spellings side by side ("
- THEORY.md:15, 97 — The claim that every piece is one of seven things, plumbing, or sugar leaves no place for the seventeen "Concepts" (question, lens, criteria, scale, threshold, predicate, provider …). — refuted: Line 15 classifies pieces of the gem (code), and the "Concepts" heading classifies terms, each of whose *Code* line already lands it inside one of the seven or under plumbing: the diagram at 11–12 writes the concepts as annotations on the seven ("measure by a question", "over a scale", "a point on i
- THEORY.md:80 vs THEORY.md:148-151, 289-290 — "The categories arrive with the question, not with the model" has two undeclared exceptions where the scale comes from the state or the record. — refuted: THEORY.md:80 contrasts question against *model* (line 78: "over a query-supplied scale" vs a classifier with fixed classes), not question against state or record; in both cited cases the scale still travels to the provider inside the request, never inside the model. The code confirms this: state.rb:
- THEORY.md:144, 146, 149, 78, 180-181, 278 — Ordinary words stand in for defined terms: "option", "label", "class" for category; "rank", "position", "index" for a level's place. — refuted: Lines 78 and 149 are not substitutions but equations that translate the outside word into the theory's term ("The classes are the scale's categories", "its labels are the scale"), and "option" at 144/146 is ordinary English in a *Code* line that mirrors the code's own `options` alias (question.rb:39
- THEORY.md:33-40, 177 vs lib/s1/state.rb:70-79 — The State is defined as rendering plus lens, but the object also carries the threshold, provider, model, timeout and owner options — none of which is evidence. — refuted: The theory never claims the Ruby object holds only rendering and lens; it says "`rendered` is the value" (THEORY.md:38), and the code matches: client.rb:15 builds the Request from `state.rendered`, with model/timeout/options passed as separate Request fields, so no setting ever reaches the provider
- THEORY.md:269-271 vs THEORY.md:277-279 — Rails is said to have the three scales as three column types, then four column types are given collapse rules. — refuted: Line 271 maps the three scales to their canonical column types; lines 277-279 map column types to retention rules — two different mappings, and the bullets themselves separate them ("the scale lives on the column" at 274 vs "the collapse rule is the column type" at 277), so a count mismatch between
- THEORY.md:8, 15, 211, 228 — "Movement", "plumbing" and "sugar" classify the whole gem yet are undefined ordinary words. — refuted: The theory's own rule (THEORY.md:5-6) is that unbolded words are ordinary English, and all three are used in their ordinary programmer senses: "plumbing" as carried infrastructure, "sugar" as syntactic sugar, "movement" as the progression the figure draws (README.md:103 uses it the same way). "Plumb
- lib/s1/distribution.rb:33-35; THEORY.md:56 vs THEORY.md:131 — `kind` returns the wire name ('noul'), the code comment calls it 'the theory's word', and the theory's own word for the kind is dichotomous/nominal/ordinal. — refuted: THEORY.md:56 already states exactly what the code does — "`kind`, the scale kind by its wire name" — and README.md:377-387 repeats it with `.kind # => "noul"` examples, and spec/s1/distribution_spec.rb:16,41 plus lib/s1/rspec.rb:38 pin that contract. The code comment at distribution.rb:33 says `kind
- lib/s1/config.rb:5-8,40,58; lib/s1/state.rb:65; lib/s1/client.rb:36; lib/s1.rb:57 — Public names with no theory or plumbing definition: Config#symbol/symbol=/symbol_name (aliases of psi), Config#cache, Config#logger, State#state (alias of rendered), S1.state, S1.to_state, S1.reset_config!, S1.on_result/hooks/clear_hooks!, S1.resolve_provider, Result#with/with_threshold/nouls/choices/scores/true?/key?, Choice#ranked, Providers::Base.settings/settings_name, Cua#close/config. — refuted: The theory's own standard (THEORY.md:5,15) is that bold terms are the only jargon and everything else is ordinary English, with every piece of the gem being one of the seven things "or plumbing under them" — it never claims the Plumbing list enumerates every public method, so `logger`, `cache`, `tim
- lib/s1/predicate.rb:60; THEORY.md:185 — Predicate#=== is listed without the qualification that for a verb it returns a distribution and so always matches. — refuted: Already addressed. THEORY.md:185's own next sentence gives the naming rule for `===` and every other application ("a verb yields one distribution per element … a `?` a boolean"), and THEORY.md:265-267 states the general consequence — "a verb in a place that expects true or false is always truthy — a
- lib/s1/state.rb:212,284-285; THEORY.md:222 — 'id — a question's key within a batch' does not say that a single-question verb's id is the wire name of its kind, which is what a Stub keyed noul:/choice:/score: relies on and what the first probe tripped over. — refuted: Already addressed in the text: README.md:907-908 states the exact rule under "Testing" ("Single-question calls are keyed by the wire name of their kind — noul (for judge, judge?, is?, same_as?), choice, score: Stub.new(noul: 0.9, choice: :billing, score: 2)"), and state.rb:210-211 repeats it beside
- lib/s1/providers/cua.rb:45-51; THEORY.md:83-84 — 'A batch is several questions on one state in one call' is one provider call from the gem's side but N sidecar round-trips for cua; harmless, unstated. — refuted: THEORY.md:83 says "one **provider** call" with provider bolded, and the dictionary entry it points to (lines 199-202) defines the provider's code as `call(Request) → Result` that "owns transport and wire format"; lines 300-303 repeat that a provider "owns one thing: the translation of measure into a
- THEORY.md:38-40, THEORY.md:276 vs typesafe-rails/lib/s1/measurable/staging.rb:229-235 and 185-195 — A later stage's state is not "the record rendered through a form plus a lens": it is the record with the earlier stages' collapses provisionally assigned, rendered AGAIN through the form, so questions in one `measure` call do not see identical evidence and the earlier verdict can appear inside `this` as well as in the lens. — refuted: The probe reproduces (stage 2 state is `{this: {transcript:"t", kind_so_far:"b"}, kind:"b"}`), but the reading of the theory is wrong: THEORY.md:39-40 ("computed once and immutable... every question asked of it sees identical evidence") is a property of one `S1::State` object, and THEORY.md:287 call
- typesafe-rails/lib/s1/measurable/staging.rb:242-262 and declarations.rb:350-354 vs THEORY.md:175-177 and 283-285 — A sequenced lens read from the row treats a float-column judge and a float-column score differently: the score path reads the stored collapse from the audit, the judge path re-collapses the stored mass at the declaration's current threshold — so the lens and the rehydrated measurement can disagree, against "the threshold travels with the measurement". — refuted: The observation reproduces (mass 0.6, audit threshold 0.5, `measurement(:lead_probability).collapse` → true, lens at redeclared 0.7 → false, `stale?` false), but it is the theory as written, not a breach: "the threshold travels with the measurement" is honoured by `measurement(:col)` carrying 0.5, w
- THEORY.md:239-241 vs typesafe-rails/lib/s1/measurable/declarations.rb:196-208, 705-712, 747 and README.md:373-378 — The same keywords `if:` / `unless:` are a **gate** only on a sequenced field; on an unsequenced field with `measure_on:` they are Rails callback conditions on the trigger, read by the callback from the last save — the theory names the first meaning only, so a reader takes every `if:` for a gate. — refuted: Both places THEORY.md uses **gate** carry the restrictor: line 239 "a sequenced field's `if:` / `unless:`" and line 293 "`if:` on a sequenced field" — so the definition is gate ⊂ `if:`, never `if:` = gate, and a reader who takes every `if:` for a gate has inverted the stated scope. The other meaning
- typesafe-rails/README.md:53, 143, 812, 882-885, 1120, 1191 vs THEORY.md:5 and 239-241 — The rails README uses "gate" in its ordinary sense for the validator ("a judgement that gates a save", "it only gates"), for a numericality validation, and for code-side decisions ("gate in code"), while the theory makes **gate** a bold term meaning only a sequenced field's `if:` / `unless:` and says it is "neither a trigger, a lens nor criteria" — the README even cites the theory's word at line 427 and then uses it otherwise. — refuted: THEORY.md:5 keys the discipline to bolding, not to the word: "Terms in **bold** … mean only what it says. Everything else is ordinary English" — so an unbolded "gate" is ordinary English by the theory's own rule, and the theory itself relies on this, writing "gated by an explicit `confident?(at)`" (
- THEORY.md:291-292 vs typesafe-rails/lib/s1/measurable/declarations.rb:213-225 — The theory lists two trigger shapes (a lifecycle moment, an attribute's change) but the DSL accepts a third — a Proc evaluated on the record after every save-commit — and `on:` beside `:validation` / `:save` / an attribute. — refuted: The Proc form is not a third kind of "when": `s1_trigger` maps it to `[:after_save_commit, proc]` and `s1_hook!` installs that proc as the callback's `if:` guard, so `measure_on: -> {…}` registers exactly the hook `measure_on: :save, if: -> {…}` does (MEASURE_ON[:save] == :after_save_commit) — a lif
- THEORY.md:237-238, 291-294 vs typesafe-rails/lib/s1/measurable/declarations.rb:227-231 and measurable.rb:259-268 — An attribute trigger pointed at a measured column (`judges :flag, measure_on: :kind` where `kind` is itself measured) is accepted at declaration but can never fire from a measurement of `kind` — faithful to "a measurement's own write is never a trigger", yet a reader would expect it to chain; the theory-faithful spelling is `after:`. — refuted: The behaviour is stated at the trigger's own definition, the very lines the claim cites: THEORY.md:237-238 "A measurement's own write is never a trigger" and :293 "the collapse of a measurement is a write the trigger does not see as a change" — generic over any trigger, not just a self-loop — and RE
- THEORY.md:272 vs typesafe-ruby/lib/s1/answer.rb:110-135 and typesafe-rails/lib/s1/measurable/staging.rb:160, writing.rb:143-144 — "an integer with a legend is ordinal" uses "legend" for the declared `{ label => integer }`, while everywhere in the code `legend` is the provider's wire-key → label map — a different object. — refuted: "legend" at THEORY.md:272 is not bold, so by the theory's own discipline it is ordinary English ("a key to what the integers mean"), not a term the code must mirror; the code's `Answer::Score#legend` is one such key (the distribution's own, wire- or position-keyed — see `stored_legend` at typesafe-r
- /Users/innocent/code/typesafe-ruby/THEORY.md:166-171 (code: lib/s1/answer.rb:45, lib/s1/distribution.rb:29) — `confident?` carries two opposite meanings under one name: provider-confidence-at-least on Choice/Score, distance-from-threshold-margin on Noul; the theory documents only the first. — refuted: The meanings are not opposite: on every kind `confident?(at)` is "this kind's confidence is at least `at`", and THEORY.md:168-169 defines the noul's confidence as "distance from the threshold is it" — so Noul(0.85).confident?(0.8) is false because its confidence is 0.35, exactly parallel to Choice(0
- /Users/innocent/code/typesafe-ruby/THEORY.md:183-186, 258 (code: lib/s1/predicate.rb:44-70) — 'Predicate' means a truth-valued function in logic (Frege's predicate IS the theory's *concept*) and a `?`-method in Ruby; the theory uses it for any unapplied question, including `ψ.score` which returns a distribution and is always truthy. — refuted: The probe shows Ruby truthiness on a *verb* in a boolean slot, not an effect of the word "predicate": the call site is `S1.predicates.judge(…)`/`ψ.judge(…)`, the class name never appears, and renaming `S1::Predicate` to `S1::Unbound` leaves `%w[a].select(&ψ.judge("Is it?"))` returning `["a"]` while
- /Users/innocent/code/typesafe-ruby/lib/s1/answer.rb:104, lib/s1/question.rb:139, lib/s1/providers/system_one_http.rb:116 (THEORY.md:204-207, 329-330) — 'score' and 'choice' each carry several senses in code, and the batch builder's `choice` is a verb, contradicting both the naming rule and the claim that nominal/ordinal distributions 'have no name of their own'. — refuted: The claim's key premise is false: `Questions#choice` (question.rb:131) measures nothing — every builder method, `q.judge`/`q.choose`/`q.score` included, adds a Question and returns the builder, so the verb/noun rule (classified by what a method returns when applied to a state) never binds the builde
- /Users/innocent/code/typesafe-ruby/lib/s1/state.rb:66 and lib/s1/result.rb:93 (THEORY.md:33-40) — 'State' names three things in code — the `S1::State` object, `State#state` (the rendered value), and `Request#state`/wire `"state"` (the rendered value) — on top of the Rails collision with state machines and `state` columns; the physics image (ψ IS the state) is also crossed by making ψ the preparation. — refuted: The "three things" are two, and the theory names both: THEORY:38 says `S1::State` is the holder and "`rendered` is the value", THEORY:214/README:873 define Request as carrying the "rendered state", and THEORY:302 says a provider "receives a state" — so `Request#state` holding `state.rendered` (clien
- /Users/innocent/code/typesafe-ruby/THEORY.md:105-109 (code: typesafe-rails/lib/s1/measurable/state.rb:19, measurable.rb:45) — An optics/functional lens focuses on a part of a structure (narrows); this lens adds a standard beside the facts (widens) — the FP reader gets the direction backwards; the earlier name was `context`. — refuted: Already addressed in the text: the bold-term definition at THEORY.md:106 opens with "evidence *added* to the state to judge against", and line 253 restates "if it changes the evidence, it is the lens" — a reader who reaches the term meets "added" before any FP association can take hold, since bold t
- /Users/innocent/code/typesafe-ruby/THEORY.md:188-191, 256-267 (Rails README.md:123) — 'Stream' in a Rails gem reads as Turbo Streams / ActionController::Live / IO; the theory means any Enumerable under one question. — refuted: Already addressed by the text's own rule: THEORY.md:5-6 says bold terms "mean only what it says," and **Stream** is bold and defined at 188-191 ("many particulars under one question"; Code: "any `Enumerable`"), so a Turbo/Live/IO reading is the reader overriding the document's stated dictionary. The
- /Users/innocent/code/typesafe-ruby/THEORY.md:61-63, 111-114 — Invoking Kant for **judgement** and **concept** while using **category** for 'a point on the scale' collides with Kant's categories (the twelve pure concepts of the understanding); the statistical sense is the intended one. — refuted: Already addressed in the text. Line 5 forecloses importing any outside sense: bold terms "mean only what it says," and Category (line 62) says "one point on the scale." The statistical sense is stated twice before a reader could reach for Kant: line 45 makes the distribution "a calibrated categorica
- /Users/innocent/code/typesafe-ruby/THEORY.md:49-52 — 'Posterior earned only under calibration' and 'credence with a warranty: it tracks frequency' misstate both disciplines: a posterior is defined by conditioning regardless of calibration (a frequentist property of a predictive distribution), and a credence is a degree of belief; calibration is a norm on it, not part of its definition. — refuted: The credence gloss already says exactly what the reader wants: "a degree of belief" is the definition, and "with a warranty: it tracks frequency" is a predicate on this particular credence (the calibration norm being met), not a redefinition of credence. "'posterior' earned only under calibration" i
- /Users/innocent/code/typesafe-ruby/lib/s1/collapsable.rb:23 (THEORY.md:161) — 'Collapsable' is a non-dictionary spelling; the -able mixin precedent (Comparable, Enumerable) does not require it. — refuted: The premise is false: "collapsable" is a dictionary-listed variant (Merriam-Webster gives "collapsible, or less commonly collapsable"; the OED and Wiktionary both carry it), so it is not a non-dictionary spelling, merely the rarer one. The Ruby precedent the claim cites actually supports the current
- /Users/innocent/code/typesafe-ruby/THEORY.md:92-93 — 'The moment information is discarded' overstates: every distribution is immutable and remains in the caller's hands after `collapse`; in QM the state after collapse is genuinely replaced, here it is not. — refuted: The sentence defines collapse as "a decision rule — the distribution becomes one category", and a decision rule is a many-to-one map whose output carries strictly less information than its input; "the moment information is discarded" is a true statement about what comes out of the rule, not a claim
- /Users/innocent/code/typesafe-ruby/THEORY.md:307-330 'Names, settled against this' (adopter: mvp/app/models/phone_call.rb:144, mvp/app/jobs/route_call_job.rb:14, mvp/config/initializers/s1.rb:6) — Every alias is a second jargon word the dictionary does not define, and the adopter uses only the legacy spellings, so the settled names never reach code. — refuted: THEORY.md:5 already scopes jargon to bold terms; no alias is ever bold, aliases appear only on *Code* lines, and the text already marks them as retained spellings ("`Subject` kept as an alias" :38, "`Answer::Base` kept as an alias" :55, "`evidence` kept as its alias" :216, "aliases *ask*, *batch*, *
- /Users/innocent/code/typesafe-ruby/THEORY.md:10-20 — Verdict: the four positions are right and the code honours them, but the theory hides two things the code already has to decide — the cost each collapse rule encodes (chosen by whoever consumes the category, not by the scale) and a third decision outcome (abstain) — and it mis-states the discipline support for the arrow names; the strongest alternative is a decision-theoretic reading that keeps every existing name. — refuted: Ownership of the collapse rule is already stated, not hidden: THEORY.md:20 ("the program does its real work between the last two"), :92 ("a decision rule"), :155-157 ("thresholds as decision rules"), :176-177 (threshold "tunable… per-state, or per-call… travels with the measurement"), :277-279 ("the
- /Users/innocent/code/typesafe-ruby/THEORY.md:310-311 (and 315-316, 86-89) — The justification for 'measure' is backwards: in both physics and measurement theory the word names the step that yields the point (the theory's collapse), and the theory itself admits this for measurement theory; only statistics ('probabilistic classification', 'soft prediction') puts the word where the theory puts it. — refuted: Already addressed in the text. THEORY.md:86-89 states outright that measurement theory yields "a point on a scale, here as a distribution over it" and grounds the verb on Stevens' scale kinds (the very ground the claim proposes to keep); 315-316 states that physics fuses measuring and collapsing whi
- /Users/innocent/code/typesafe-ruby/THEORY.md:91-95, 173-177 — Collapse is called 'a decision rule' but the theory never says what each rule decides *about* — the cost of a wrong category — which is the one fact that explains why the threshold is tunable, why the most-likely category is the default, and why Rails lets the column pick the rule. — refuted: The theory already says who owns the rule and what tuning it means: collapse "belongs to the program" (THEORY.md:315), a category is "what the rest of the program acts on" (:62), and calibration states the threshold's meaning in ordinary English — "a mass of 0.7 means seven in ten are true" (:154),
- /Users/innocent/code/typesafe-ruby/THEORY.md:61-63, 91-95, 223 — The movement's last position has no room for the outcome the code and README recommend most — declining to collapse and handing off — so 'abstain' lives in plumbing while the diagram says every judgement ends in one category. — refuted: The theory already houses abstention at the distribution position, not in a missing fifth one: THEORY.md:18-20 says "the program does its real work between the last two", the Distribution entry (THEORY.md:52-54) names the distribution "a belief state awaiting a decision rule — the reason to keep it"
- /Users/innocent/code/typesafe-ruby/THEORY.md:296-297 — 'Writing the answer into the row is the collapse' conflates the rule with the action: the code collapses by column type without any write (assign_measure; a stage's provisional assignment) and has three separate implementations of 'as the column would hold it'. — refuted: The text does not conflate rule and act: THEORY.md:277 names the rule ("the collapse rule is the column type"), 296 names the moment ("writing ... is the collapse"), and the dictionary sanctions both senses (91-93: "a decision rule ... the moment information is discarded"; 315: two events, the secon
- /Users/innocent/code/typesafe-ruby/THEORY.md:286-288 — A sequenced field collapses mid-movement and feeds the category back in as evidence — the diagram is linear and the theory calls collapse 'the moment to postpone', so the one place the program collapses early on purpose is unnamed as such. — refuted: The bullet already names it three ways ("judged against an earlier category", "that field's collapse enters the later state", "relative to an earlier verdict"), and the **stage** entry (THEORY.md:232) defines a stage as the questions asked "once the fields they come `after:` are collapsed" — the col
- /Users/innocent/code/typesafe-ruby/THEORY.md:10-13, 68-71 — Prepare is drawn as one arrow with one input, but the code separates it into render (once) and lens (any number of times over the same facts), and the state is a pair the diagram flattens; the question and the provider are likewise inputs the diagram shows only as a caption. — refuted: The pair is already defined in the text: State *Theory* is "the thing as rendered, fixed, with its lens attached" (THEORY.md:35), and the Plumbing entry for **facts** names both halves explicitly — "the evidence as rendered, alone… `State#lens` is the Hash beside it, `{}` with none" (215-217), with
- /Users/innocent/code/typesafe-ruby/THEORY.md:176-177 (and lib/s1/state.rb:58, 69) — The reason the code refuses `threshold:` on a verb — only a collapse takes one — applies equally to the prepare position, where the code accepts it; the actual rule is 'defaulted upstream, applied at the collapse', and the theory should say that instead of listing sites. — refuted: The claim reads "threshold: only applies to a collapse" as "is accepted only by a collapse," but the message states where the threshold is applied, and a State accepting one does not apply it — it defaults it for the collapse it travels to, which is exactly what THEORY.md:176-177 already says ("the
- /Users/innocent/code/typesafe-ruby/THEORY.md:83 — P(category | state, question) omits the provider, yet the provider changes the distribution (calibration is per provider), the Result records it, and Rails keys its cache and splits its batches on it. — refuted: The text already addresses this: the Provider entry (THEORY.md:199-202) defines a provider as "an implementation of *measure*", line 87-88 says the distribution is "a conditional categorical distribution, estimated", and lines 301-305 say the three providers are "three instances of the same arrow" —
- README.md:457-466 and lib/s1/question.rb:9-10 vs THEORY.md:249-254 and lib/s1/state.rb:150 — The endorsed verification pattern puts a particular (`extracted_value`) inside the question, which the theory's own two-adjustments rule classifies as evidence, and which `same_as` correctly puts in the state. — refuted: The two-adjustments rule, applied by its own test, does not classify `extracted_value` as evidence: swapping "4471" for "9999" leaves `source_text` (what is judged) untouched and changes what is asked, so it sits on the question side, and THEORY.md:122-125 already reserves that seat — a concept "inc
- THEORY.md:225-226 (wire) and THEORY.md:272; lib/s1/providers/base.rb:49, lib/s1/providers/system_one_http.rb:114, lib/s1/providers/stub.rb:46, lib/s1/answer.rb:110 — `legend` is a load-bearing wire/normalizer term (the index → label map a score is keyed by) that the theory never defines; its wire list is also incomplete. — refuted: THEORY.md:272 uses "legend" in its ordinary-English sense (a key mapping integer codes to labels), and that is the same sense as the wire field `{ "0" => "Cosmetic", ... }`, not a different one — so there is no ambiguity, and by the theory's own rule (line 5) a non-bold word in ordinary English need
- THEORY.md:38-40 and :122-125 vs lib/s1/state.rb:147-154 — A relational question builds a state of two particulars (`this`, `other`) beside the lens; the theory defines a state as one particular and its lens, so `other` is neither facts nor lens. — refuted: The premise "`other` is neither facts nor lens" is false by the theory's own definitions: a lens is "evidence added to the state to judge *against* … which makes the judgement relative" (THEORY.md:105-107), `against` is the code's alias of `given` (state.rb:105), and the discriminating rule at :252-
- THEORY.md:170-171 and lib/s1/distribution.rb:31 vs support/laya_server.py:11-12 and typesafe-rails/README.md:1278-1284 — `confident?(at)` is true whenever a provider reports no confidence, so for a Laya score every routing gate passes — the theory's routing convention is vacuous on a shipped provider. — refuted: The factual premise is false: Laya's own `agent.py` score branch emits `"confidence": conf_score` (computed as `confidence_from_probs(p, k)`, 1 − H(p)/log k) exactly as the choice branch does, and laya_server.py passes answers through untouched — the docstring's "score / probabilities" is an abbrevi
- THEORY.md:158-159 vs README.md:945-955 and lib/s1/providers/stub.rb:43,49 — By the theory's own sentence the Stub is not a provider — its numbers are canned, not calibrated — yet the README counts it among the four providers and it produces collapsables with a fabricated confidence of 1.0. — refuted: The calibration clause governs numbers a provider produces as measurements; the Stub produces none — it relays the test author's stipulation ("suppose a calibrated provider reports 0.9"), which is what a test double is, so the axiom holds for it by construction rather than being violated. That statu
- THEORY.md:83 vs lib/s1/providers/cua.rb:45-51 — 'One provider call' per batch is not what cua does — it makes one sidecar round-trip per question. — refuted: "Provider call" in THEORY.md:83 is the code-level term for the contract `#call(Request) -> Result` (lib/s1/providers/base.rb:5-7), and `Client#measure` invokes `impl.call(request)` exactly once per batch for every provider, Cua included (lib/s1/client.rb:28) — so the sentence is literally true of cu
