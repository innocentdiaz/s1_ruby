AllCops:
  TargetRubyVersion: 3.2
  NewCops: enable
  SuggestExtensions: false

Style/StringLiterals:
  EnforcedStyle: double_quotes

Style/StringLiteralsInInterpolation:
  EnforcedStyle: double_quotes

Style/Documentation:
  Enabled: false

Style/OptionalBooleanParameter:
  Enabled: false

Layout/LineLength:
  Max: 140

Metrics/AbcSize:
  Max: 30

Metrics/MethodLength:
  Max: 20

Metrics/CyclomaticComplexity:
  Max: 15

Metrics/PerceivedComplexity:
  Max: 15

Metrics/ParameterLists:
  Max: 6
  CountKeywordArgs: false

Metrics/BlockLength:
  Exclude:
    - spec/**/*
    - lib/s1/rspec.rb   # the shared conformance examples
    - s1.gemspec

# The noul DSL takes `true:` / `false:` clarification kwargs by design.
Lint/BooleanSymbol:
  Exclude:
    - spec/**/*

Lint/ConstantDefinitionInBlock:
  Exclude:
    - spec/**/*

# Delegators: `**kw` is the whole point; `confident?(at)` reads as English.
Naming/MethodParameterName:
  AllowedNames: [kw, id, a, q, e, at]

# Predicates is a bag of builders called as S1.predicates.judge(...) or via ψ: extend self, aliased once.
Style/ModuleFunction:
  EnforcedStyle: extend_self

# `collapse` returns a boolean by design: it is the named form of the `?` methods.
Naming/PredicateMethod:
  AllowedMethods: [collapse]

Metrics/ClassLength:
  Max: 120
